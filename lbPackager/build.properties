#Product version - major release
vMajor=6

#Product version - minor release
vMinor=6

#Product version - patch level
vPatch=5

#Product version - candidate status (e.g. a= alpha, b=beta, rc=release candidate )
# Snapshots require a -SNAPSHOT suffix (ex. ".02-SNAPSHOT")
vCStat=.RC2-SNAPSHOT


#Product build id
vBuild=${vMajor}.${vMinor}.${vPatch}${vCStat}
#vBuild=${vMajor}
